import {
  colors,
  feedCard,
  feedTierFilter,
  layout,
  postDetail,
  radius,
  shadows,
  spacing,
  typography,
} from './tokens';

export const theme = {
  colors,
  spacing,
  radius,
  typography,
  shadows,
  layout,
  feedCard,
  feedTierFilter,
  postDetail,
};
