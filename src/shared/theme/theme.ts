import {
  colors,
  feedCard,
  layout,
  radius,
  shadows,
  spacing,
  typography,
} from './tokens';

export const theme = {
  colors,
  spacing,
  radius,
  typography,
  shadows,
  layout,
  feedCard,
};
